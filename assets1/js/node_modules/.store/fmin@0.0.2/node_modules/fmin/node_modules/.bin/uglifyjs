#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/E:/dabtvis/assets1/js/node_modules/.store/uglify-js@2.8.29/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/E:/dabtvis/assets1/js/node_modules/.store/uglify-js@2.8.29/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../uglify-js@2.8.29/node_modules/uglify-js/bin/uglifyjs" "$@"
else
  exec node  "$basedir/../../../../../uglify-js@2.8.29/node_modules/uglify-js/bin/uglifyjs" "$@"
fi
